<template>
  <div class="text-center">
    <v-bottom-sheet
      v-model="sheet"
      inset
      persistent
    >
      <template v-slot:activator="{ on, attrs }">
        <v-btn
          color="orange"
          dark
          v-bind="attrs"
          v-on="on"
        >
          Open Inset
        </v-btn>
      </template>
      <v-sheet
        class="text-center"
        height="200px"
      >
        <v-btn
          class="mt-6"
          text
          @click="consoleDialogs"
        >
          getDialogList
        </v-btn>
        <v-btn
          class="mt-6"
          text
          color="error"
          @click="sheet = !sheet"
        >
          close
        </v-btn>
        <div class="my-3">
          This is a bottom sheet using the inset prop
        </div>
      </v-sheet>
    </v-bottom-sheet>
  </div>
</template>

<script lang="ts">
import { defineComponent } from '@nuxtjs/composition-api';

export default defineComponent({
  name: 'CloseSpecificBottomSheet',
  data: () => ({
    sheet: false
  }),
  methods: {
    consoleDialogs() {
      console.log(this.$dialog.getAllOpenDialog());
    }
  }
});
</script>

<style scoped>

</style>
